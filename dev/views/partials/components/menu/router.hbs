<section>
    <nav class="og-nav-tabs-fusion">
        <ul class="ul">
            {{#each navPages}}
                <li class="tabs"><a href="{{this.slug}}" class="a">{{this.name}}</a></li>
            {{/each}}
        </ul>
    </nav>
</section>
